apiVersion: v1
kind: Service
metadata:
  name: minio-edge1
  labels:
    app: minio-edge1
    service: minio-edge1
spec:
  ports:
  - name: http
    port: 9090
    targetPort: 9090
  - name: api
    port: 9000
    targetPort: 9000
  selector:
    clustername: edge1
