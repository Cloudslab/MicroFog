apiVersion: v1
kind: Service
metadata:
  name: minio-cloud2
  labels:
    app: minio-cloud2
    service: minio-cloud2
spec:
  ports:
  - name: http
    port: 9090
    targetPort: 9090
  - name: api
    port: 9000
    targetPort: 9000
  selector:
    clustername: cloud2

