apiVersion: v1
kind: Pod
metadata:
  name: controller-edge1
  labels:
    app: control-engine
    cluster: edge1
spec:
  containers:
  - name: controller-edge1
    image: samodha/microfog:latest
    resources:
      requests:
        cpu: "100m"
    imagePullPolicy: Always
    ports:
    - containerPort: 8080
  serviceAccountName: controller-serviceaccount 
  
